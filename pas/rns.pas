{$I RNS.H}
Program Rns(Input, Output);

Uses Graph,
    crt,
    Dos,
    initsc,
    UserInt,
    EditUnit,
    menutyp,
    helpunit,
    userexit;

Begin
    IniIniColors;
    UseTopMenu;
    TextBackground (black);
    TextColor (white);
    ClrScr;
End.
